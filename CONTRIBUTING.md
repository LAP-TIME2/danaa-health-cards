# Contributing

1. Use small pull requests.
2. Do not add automatic transcript collection.
3. Do not store raw tokens in files.
4. Add tests for security-sensitive changes.
5. Keep user-facing text clear for non-developers.

Recommended checks:

```bash
npm run typecheck
npm test
```
